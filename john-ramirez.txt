target : https://codepen.io/BritHemming/full/jONmxOm 

forked : https://codepen.io/Emperor-D-Pengin/pen/oNWMQOj?editors=1100

    1. What is Semantic HTML? 
            Is something that explains at a glance what it is used for or what it contains.
    
    2. What is HTML used for? 
            Is used to create websties by explaining to a Browser how to present the information given
    
    3. What is an attribute and where do we put it? 
            an Attribute is something that can give more information about its element, It can be found
            on the element (ex.  <img src="img_girl.jpg" width="500" height="600"> )
    
    4. What is the h1 tag used for? How many times should I use it on a page?
            it is used to Create the Biggest Title and should be used once to describe the pages content.
    
    5. Name two tags that have required attributes
            <a>
            <img>
    
    6. What do we put in the head of our HTML document? 
            it contains information about the page, such as <title>, links to any javascript or CSS scripts.
    
    7. What is an id? 
            it is something u cannot have more than 1 in a HTML file. it is a way to tag an element of a page as unique.
    
    8. What elements can I add an id to?
            any element but different id Names 
    
    9. How many times can I use the same id on a page? 
            once
    
    10. What is a class? 
            a class is a way to mark multiple HTML elements to be treated or modified the same way.
    
    11. What elements can I add a class to? 
            any element
    
    12. How many times can I use the same class on a page? 
            as many times as u want
    
    13. How do I get my link to open in a new tab?
            by setting the <a> tag attribute "target" to "_blank" (target="_blank")
    
    14. What is the alt attribute in the image tag used for? 
            in case the src attribute does not work or the image cannot be found. it will display whatever is in the alt attribute
    
    15. How do I reference an id?
            using the # sign (ex. #id_name)
    
    16. What is the difference between a section and a div
            section means that the content all relates to one another. or a single theme
            while div does not have a meaning aside from what its class tells us about it.
    
    17. What is CSS used for? 
            to more freely and specifically make visual changes to how the HTML data is dispalyed look wise.
    
    18. How to we select an element? Example - every h2 on the page
        h2{

        }
    
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
        a class can be used multiple times on a webpage an id cannot
    
    20. How do we select classes in CSS?
        .class-name{

        }
    
    21. How do we select a p element with a single class of “human””?
            .human p{

            }
    
    22. What is a parent child selector? When would this be useful? 
            it is a way to apply CSS style to an element by addressing its container and selecting it from there.
    
    23. How do you select all links within a div with the class of sidebar?
            .sidebar div a{

            }

    24. What is a pseudo selector?
            it is used to change the state of the element under different circumstance ie. Hovering

    25. What do we use the change the spacing between lines?
            line-height

    26. What do we use to change the spacing between letters?
            letter-spacing

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
            text-transform

    28. How do I add a 1px border around my div that is dotted and black?
            border: dotted 1px black;

    29. How do I select everything on the page? 
            *{

            }

    30. How do I write a comment in CSS?
            /*  */

    31. How do I find out what file I am in, when I am using the command line?
            pwd

    32. Using the command line - how do I see a list of files/folders in my current folder?
            ls

    33. How do I remove a file via the command line? Why do I have to be careful with this?
            git rm <file> it wont ask u if u are ok with it

    34. Why should I use version control? 
            in case something breaks u can always go back

    35. How often should I commit to github?
            often enough

    36. What is the command we would use to push our repo up to github?
            git push -u origin branch-name

    37. Walk me through Lambda's git flow.
            1. Fork
            2. Clone
            3. copy HTTP address
            4. create Directory or Folder
            5. cd to directory
            6. Clone : git clone <link-repo>
            7. cd into the repo
            8. create a new branch: git checkout -b 'firstname-lastname'
            after changes
            9. git add . (will add cahnges to commi)
            10. git commit -m "message about changes"
            11. git push -u origin branch-name
            12. in github go to compare and pull.
            13. make sure it is comparing the main branch repo that was forked to the changes.
            14. press create pull request.